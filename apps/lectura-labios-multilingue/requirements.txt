# Lectura de labios (VSR) — versiones VERIFICADAS EJECUTANDO el preprocesado completo
# (Python 3.11, CPU). En Colab NO reinstales torch/torchvision/torchaudio: usa los suyos
# (el notebook trae parches para que el repo funcione con torch moderno).

# --- CRÍTICO: mediapipe con API "solutions" (las versiones >=0.10.30-ish la eliminaron) ---
mediapipe==0.10.9        # verificada; si tu Python no tiene wheel, usa mediapipe==0.10.14

# --- Dependencias del repo mpc001 (verificadas en estas versiones) ---
hydra-core==1.3.4
av==18.0.0               # también hace de sustituto de torchvision.io.read_video (parche)
scikit-image==0.26.0
scipy==1.17.1
six==1.16.0
numpy==2.4.6

# --- Tokenizador del modelo de lenguaje ---
sentencepiece==0.2.2

# --- Descarga de pesos desde Google Drive ---
gdown==6.1.0

# --- Interfaz web con enlace público ---
gradio

# --- Stack de inferencia verificado (en Colab ya vienen; NO reinstalar allí) ---
# torch==2.13.0
# torchvision==0.28.0    # OJO: ya no trae read_video -> el notebook lo parchea con PyAV
# torchaudio==2.11.0
