# Motor REAL de referencia: remove-ai-watermarks (Apache-2.0), de wiltodelta.
#   https://github.com/wiltodelta/remove-ai-watermarks  ·  https://pypi.org/project/remove-ai-watermarks/
#
# Perfiles (elige según lo que necesites; todo lo de aquí es CPU salvo 'diffusion'):
#   remove-ai-watermarks            -> identify + strip metadatos (C2PA/EXIF/XMP/IPTC). CPU, ligero.
#   remove-ai-watermarks[visible]   -> + quitar marcas VISIBLES de generador (opencv). CPU.
#   remove-ai-watermarks[detect]    -> + detección DWT-DCT (SD/SDXL/FLUX). CPU.
#   remove-ai-watermarks[diffusion] -> + romper marca INVISIBLE/SynthID. REQUIERE GPU (CUDA).
#
# Por defecto instalamos el perfil CPU completo útil para este servicio:
remove-ai-watermarks[visible,detect]>=0.26

# Para el borrado de marca invisible (SynthID) en un worker con GPU, además:
#   pip install "remove-ai-watermarks[diffusion]"   # torch CUDA, diffusers, etc.
