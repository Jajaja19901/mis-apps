# VIGÍA · servidor-cerebro en la nube — dependencias
ultralytics>=8.3.0      # YOLO11 (detección de objetos)
fastapi>=0.110.0        # servidor web ligero
uvicorn>=0.29.0         # arranca el servidor
pydantic>=2.0.0         # valida la petición
pillow>=10.0.0          # abre el JPEG que manda el móvil
